import 'package:flutter/material.dart';

class MemberRegisterFormButton extends StatelessWidget {
  MemberRegisterFormButton();
  @override
  Widget build(BuildContext context) {
    return FlatButton
    (
      child: Text("Kayıt"),
      onPressed: (){

      },
    );
  }
}